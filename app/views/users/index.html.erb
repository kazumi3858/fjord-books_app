<div class="all-users">
  <h1><%= t("views.common.all_users") %></h1>

  <ul class="users">
    <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
    <% end %>
  </ul>
  <%= paginate @users %>
  <%= link_to t('views.common.back'), books_path %>
</div>
