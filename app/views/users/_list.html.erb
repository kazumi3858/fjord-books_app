<div class="follow-relationship-users">
  <ul>
    <% unless users.nil? %>
      <% users.each do |user| %>
        <li><%= link_to "#{user.name}", user_path(user) %></li>
      <% end %>
    <% end %>
  </ul>
</div>
